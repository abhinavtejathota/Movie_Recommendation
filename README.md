# Movie_Recommendation
Movie recommendation system based on mood(Failed attempt)
The dataset was taken from TMdb(The Movie database), it has lot of empty and inappropriate values, data was preprocessed as much as possible but there was a lot of noise so it was quite time consuming and has caused further errors as well, feature extraction was done on overview, using an already present model but was not good, this caused a lot of errors in recommending properly.
